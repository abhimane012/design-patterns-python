class MySQLDatabase:
    def insert(self, record):
        print(f"Inserting {record} into MySQL Database")

    def delete(self, record):
        print(f"Deleting {record} into MySQL Database")

    def update(self, record):
        print(f"Updating {record} into MySQL Database")

    def select(self, record):
        print(f"Selecting {record} into MySQL Database")
