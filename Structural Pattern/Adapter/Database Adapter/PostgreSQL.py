class PostgreSQLDatabase:
    def insert(self, record):
        print(f"Inserting {record} into PostgreSQL Database")

    def delete(self, record):
        print(f"Deleting {record} into PostgreSQL Database")

    def update(self, record):
        print(f"Updating {record} into PostgreSQL Database")

    def select(self, record):
        print(f"Selecting {record} into PostgreSQL Database")
